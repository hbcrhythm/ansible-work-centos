---
# This playbook deploys env

- name: deploy common
  hosts: vimservers
  remote_user: root

  roles:
    - common

  tags:
    - common

- name: deploy work environment
  hosts: vimservers
  remote_user: root

  roles:
    - work

  tags:
    - work